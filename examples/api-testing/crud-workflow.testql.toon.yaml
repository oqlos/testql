# SCENARIO: CRUD Workflow Test
# TYPE: api
# VERSION: 2

CONFIG[3]{key, value}:
  targets, {'api': {'base_url': 'http://localhost:8101'}}
  api.base_url, http://localhost:8101
  base_url, http://localhost:8101
API[4]{method, endpoint, status}:
  POST, /api/v3/scenarios, 201
  GET, /api/v3/scenarios/${scenario_id}, 200
  PUT, /api/v3/scenarios/${scenario_id}, 200
  DELETE, /api/v3/scenarios/${scenario_id}, 204
CAPTURE[1]{step, var, from}:
  1, scenario_id, data.id
