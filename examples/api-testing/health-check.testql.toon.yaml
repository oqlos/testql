# SCENARIO: API Health Check
# TYPE: api
# VERSION: 2

CONFIG[4]{key, value}:
  targets, {'api': {'base_url': 'http://localhost:8101', 'timeout_ms': 5000}}
  api.base_url, http://localhost:8101
  base_url, http://localhost:8101
  api.timeout_ms, 5000
API[3]{method, endpoint, status}:
  GET, /api/health, 200
  GET, /api/v3/data/devices, 200
  GET, /api/v3/scenarios, 200
