# SCENARIO: API Contract Tests
# TYPE: api
# VERSION: 1.0

CONFIG[2]{key, value}:
  base_url, http://localhost:8101
  api_version, v3

API[4]{method, endpoint, expected_status}:
  GET, ${base_url}/api/${api_version}/health, 200
  GET, ${base_url}/api/${api_version}/devices, 200
  POST, ${base_url}/api/${api_version}/scenarios, 201
  DELETE, ${base_url}/api/${api_version}/scenarios/{id}, 204

ASSERT[2]{field, operator, expected}:
  content_type, contains, application/json
  status, <, 500
