# SCENARIO: Variables and Interpolation
# TYPE: api
# VERSION: 1.0

CONFIG[3]{key, value}:
  base_url, http://localhost:8101
  timeout_ms, 5000
  api_key, test-key-123

API[2]{method, endpoint, expected_status}:
  GET, ${base_url}/api/health, 200
  GET, ${base_url}/api/devices, 200

