# SCENARIO: test-gui-all.iql — Master GUI test suite — runs all module GUI tests
# TYPE: api
# VERSION: 1.0

# ── Konfiguracja ──────────────────────────────────────
CONFIG[2]{key, value}:
  encoder_url,  http://localhost:8105
  api_url,  http://localhost:8101

INCLUDE[1]{file}:
  test-gui-connect-id.iql

INCLUDE[1]{file}:
  test-gui-connect-test.iql

INCLUDE[1]{file}:
  test-gui-connect-workshop.iql

INCLUDE[1]{file}:
  test-gui-connect-manager.iql

INCLUDE[1]{file}:
  test-gui-connect-config.iql

INCLUDE[1]{file}:
  test-gui-connect-reports.iql
