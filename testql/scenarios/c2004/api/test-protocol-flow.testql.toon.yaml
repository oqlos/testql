# SCENARIO: test-protocol-flow.tql — protocol creation and completion flow
# TYPE: api
# VERSION: 1.0

# ── Wywołania API ─────────────────────────────────────
API[3]{method, endpoint, status}:
  POST,  /api/v3/data/protocols",  200
  GET,  /api/v3/data/protocols,  200
  GET,  /api/v3/data/protocols?limit=1&sort=created_at:desc,  200
