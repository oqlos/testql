# SCENARIO: connect-config-users.tql — GUI test for config/users view
# TYPE: gui
# VERSION: 1.0

# ── Nawigacja UI ──────────────────────────────────────
NAVIGATE[1]{path, wait_ms}:
  /connect-config-users,  500

FLOW[1]{command, target, meta}:
  assert_visible,  [data-view='config-users'],  -

FLOW[1]{command, target, meta}:
  click,  [data-action='add-user'],  -

WAIT[1]{ms}:
  300

FLOW[1]{command, target, meta}:
  assert_visible,  [data-dialog='user-form'],  -

FLOW[1]{command, target, meta}:
  click,  [data-action='cancel'],  -

WAIT[1]{ms}:
  200
