# SCENARIO: connect-id-barcode.tql — GUI test for ID barcode scanning view
# TYPE: gui
# VERSION: 1.0

# ── Nawigacja UI ──────────────────────────────────────
NAVIGATE[1]{path, wait_ms}:
  /connect-id-barcode,  500

FLOW[1]{command, target, meta}:
  assert_visible,  [data-view='id-barcode'],  -

FLOW[1]{command, target, meta}:
  input,  #barcode-input,  -

WAIT[1]{ms}:
  200

FLOW[1]{command, target, meta}:
  click,  [data-action='scan'],  -

WAIT[1]{ms}:
  300
